# Installation

Install `cypherglot` from PyPI:

```bash
uv pip install cypherglot
```

Install from source in editable mode:

```bash
uv pip install -e .
```

For local development with docs and test tooling:

```bash
uv sync --group test --group docs
```

## Python support

`cypherglot` targets Python `3.10+`.
